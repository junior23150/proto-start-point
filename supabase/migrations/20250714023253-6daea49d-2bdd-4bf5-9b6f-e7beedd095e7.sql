-- Configuração de secrets do Supabase
-- IMPORTANTE: Todos os secrets devem ser configurados via Supabase Dashboard
-- Acesse: Project Settings > Functions > Secrets

-- Secrets necessários:
-- WHATSAPP_ACCESS_TOKEN: [configurar via dashboard]
-- WHATSAPP_PHONE_NUMBER_ID: [configurar via dashboard]
-- WHATSAPP_VERIFY_TOKEN: [configurar via dashboard]
-- OPENAI_API_KEY: [configurar via dashboard]

SELECT 'Secrets must be configured via Supabase dashboard under Project Settings > Functions' as security_note;